{
  "name": "redux-callable",
  "version": "1.1.0",
  "description": "Creates reducers that can be called as they are, without dispatching action objects",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "prepublishOnly": "stylecheck && lint && test && build",
    "lint": "tslint -c tslint.json \"src/**/*.ts\"",
    "stylecheck": "prettier --check \"src/**.ts\"",
    "coverage": "jest --collect-coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/artss/redux-callable.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/artss/redux-callable/issues"
  },
  "homepage": "https://github.com/artss/redux-callable#readme",
  "devDependencies": {
    "@types/jest": "^24.0.12",
    "jest": "^24.8.0",
    "prettier": "1.17.0",
    "ts-jest": "^24.0.2",
    "tslint": "^5.16.0",
    "tslint-config-airbnb": "^5.11.1",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.4.5"
  },
  "peerDependencies": {
    "redux": "*"
  }
}
